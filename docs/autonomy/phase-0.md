### Phase 0 — Class removers, and the measurements everything else is priced against

**Capability:** whole classes of failure become unreachable, and the three numbers the rest of the
plan spends are taken **before** anything is changed. Every item here is hours, not days, and each
becomes materially more expensive to retrofit once the runner is written against today's contracts.

> **STATUS 2026-08-03: PHASE 0 IS CLOSED.** All three items the block below named as outstanding
> have produced their numbers, and this phase's own rule — a check that produced no number has not
> been done — is now satisfied for every row.
>
> - **0.11 — DONE** (`build-manager`, 2026-08-02). ≈5–7 qualifying leads/day from the 44 boards;
>   the board count for 999/day is written into §7 R-8 and §2. Independently recomputed from
>   `board_stats` on 2026-08-03 at `5d5da52` (4.7–6.1/day, ~7,200–9,400 boards) and it agrees.
> - **B1 — DONE**, `docs/measurements.md` B1, with its three `file_sha1`s.
> - **0.13 — DONE, and it refuted half of its own premise** (`implementer`, `5d5da52`, M10).
>   77 → **91 of 158** leads carry a gate-usable `apply_url`, at zero network cost. But the
>   aggregator half is **not recoverable**: 0 of 21 sampled adzuna/jobicy links resolved, 11 of
>   them 403. Those 60 leads are a supply-side problem (Phase 6.1), not a canonicalization one.
>   The row's "~40% of leads" figure was right about the size of the loss and wrong about this
>   item's ability to recover it.
>
> **Phase 5 is therefore unblocked.** Its trust gate now has a canonicalized `apply_url` to read
> for 58% of the store, and a measured, named reason for the other 42%.
>
> **The 2026-08-02 block is kept below verbatim, because it is the diagnosis that produced the
> work and not a description of today.**
>
> ---
>
> **STATUS 2026-08-02: PHASE 0 IS NOT CLOSED.** It was reported closed and acted on as closed; it is
> not. **Every code item has landed** — verified against the tree at `0b6db30`, not against a
> report: `assertPageOrigin` in `authorize.mjs` (0.1), `labelFlag` in `fill-plan.mjs` (0.2),
> `safeText` in the `plan_defer` push (0.3, `06ffaff`), `COPY_ALLOWLIST` in `auth-sync.mjs` (0.4),
> `hostOf(scan.url)` in `field-cache.mjs`'s `fingerprint` basis (0.5), the
> `identity-verification` defer kind in `fill-plan.mjs` (0.6),
> `tests/apply/answer-bank.test.mjs`'s "0.7: a plus-aliased address…" test (0.7),
> `--browser-fill` in `bench-apply.mjs` (0.9), and the committed
> `ashby-step1`/`lever-step1` scans plus `--latency` and `--origins` in the fixture server (0.10).
>
> **Three items produced nothing, and this phase's own rule says a check that produced no number has
> not been done:**
>
> - **0.11 — OUTSTANDING.** No sweep number exists anywhere in this plan or in
>   `docs/measurements.md`. The board count for 999 qualifying leads/day is still unwritten in §7 R-8
>   and §2. Phase 1.8 already had to anchor its 30-day projection on the user's `per_day_max`
>   instead, and said so. A sweep is running now; **do not fill this in with an estimate.**
> - **0.13 — NOT STARTED.** `grep -n "canonical" scripts/leads/enrich.mjs` returns nothing. The
>   trust gate (§4.8 precondition 1) is specified against a canonicalized `apply_url` that nothing
>   produces, so ~40% of leads would be rejected at the gate for carrying an aggregator host.
> - **B1 — MISSING.** `docs/measurements.md` carries no B1 entry and no `fill-plan.mjs` /
>   `answer-bank.mjs` / `field-cache.mjs` `file_sha1` triple. 0.9's harness leg exists; the baseline
>   it was built to capture does not, which is exactly the C11 failure — fill-path changes have
>   already landed and there is nothing to compare them against.
>
> 0.12 **is** done (`4650a00`, M7). Phase 1 and Phase 2 were opened ahead of this; that is a
> sequencing fact to record, not a thing to undo.
>
> **The State column now says what is true, and `LANDED` is deliberately weaker than `DONE`.**
> `LANDED` means the code is in the tree at `0b6db30` and was read there; it does **not** mean a test
> was run for this pass or that the item's measurement obligation is discharged. `DONE` is reserved
> for a row that names its commit. `PARTIAL` means some of the row shipped and the rest is named
> above.

| #        | Work item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Owner                       | State       |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ----------- |
| 0.1      | `consumeSubmitToken` **shall** take a required `pageUrl` and throw `TokenError` unless its origin equals `token.apply_url`'s origin. The token already carries `apply_url` and nothing compares it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `implementer`               | LANDED      |
| 0.2      | `submitReadiness` **shall** return false when any plan item or defer entry carries a `labelFlag`, and `authorizeSubmit` **shall** mirror the key beside its existing `plan_defer` check, so relaxing one cannot widen the gate. **Budget: declare before pickup** (§0.9).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `implementer`               | LANDED      |
| 0.3      | Page-derived labels **shall** pass through `sanitizeUntrusted` before entering a defer reason or the run JSONL. **DONE 2026-08-01** (`w4-autonomy`, committed `06ffaff`). The claim this row used to make — that `authorize.mjs:305-315` builds the reason from the raw third-party label — was stale twice over: the line was `:342-352` at `9e0a159`, and the behaviour is fixed. At HEAD it is `safeText(d?.label ?? d?.k ?? "?", 120)` in `authorize.mjs`'s `plan_defer` push — cite the symbol; that line number has already moved once since `74c7970`. Scrubbing lives in `scripts/auto/untrusted-text.mjs`, which keeps machine-shaped values verbatim and records a finding instead of rewriting them.                                                                                                                                                                                                                                                                           | `implementer`               | DONE        |
| 0.4      | `auth-sync`'s copy filter **shall** be inverted from a denylist of caches to an allowlist (Cookies, Local Storage, Local State, minimal Preferences). `SKIP_DIRS` in `auth-sync.mjs` excluded only caches, so saved site passwords (`Login Data`), autofill payment cards (`Web Data`), History and Bookmarks were copied into a profile that will visit hundreds of third-party pages. **Landed:** `SKIP_DIRS` no longer exists; `COPY_ALLOWLIST` replaced it and the file's header records why.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `implementer`               | LANDED      |
| 0.5      | The field-cache fingerprint **shall** include the registrable host. `field-cache.mjs`'s `fingerprint()` built `atsId + "\|" + labels.join("\n")`, cross-tenant by construction. **Landed:** the basis now interpolates `hostOf(scan.url)`, so the cited shape is gone. **Budget: declare before pickup — this invalidates every existing cache entry and forces a re-probe on the next application per board, and nobody currently knows whether a probe costs 87.89ms or 6.8s.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `implementer`               | LANDED      |
| 0.6      | An **identity-verification wall** (Real Talent / CLEAR selfie, quality-tier challenge) **shall** be a named defer kind, distinct from CAPTCHA, distinct from failed-fill, and explicitly **not** a malfunction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `implementer`               | LANDED      |
| 0.7      | Name/email/phone **shall** be emitted byte-identically on every form, sourced from `profile.yaml` only, asserted by a test that fails on format drift or plus-aliasing. **Budget: declare before pickup.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `implementer`, `qa`         | LANDED      |
| 0.8      | The in-flight wave — `authorize.mjs` capability token, durable attempted row, `LEADS_LOCK` in `find-jobs.mjs`, dead consent-allowlist grant branch deleted — **lands here.** Do not re-propose it; build on it. **`doc-scribe` re-runs §0.2's table with `file_sha1`s once it commits.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `implementer`, `doc-scribe` | LANDED      |
| **0.9**  | **NEW (C11).** `bench-apply.mjs` **shall** gain a `--browser-fill` leg running `fill-engine.mjs` through the existing `clockedPage()` recorder against the loopback fixture, including one real file upload, reporting fill wall, unconditional sleep and post-upload remount as **separate columns** — and fixing M6, where a run reporting `ok:2 failed:1 deferred:3` still passes. Baseline **B1** captured at a clean `git worktree` of `fa192a1` and written to `docs/measurements.md` with `file_sha1` for `fill-plan.mjs`, `answer-bank.mjs`, `field-cache.mjs`.                                                                                                                                                                                                                                                                                                                                                                                                                   | `qa`                        | PARTIAL     |
| **0.10** | **NEW (C11).** `ashby-step1.scan.json` and `lever-step1.scan.json` **shall** be committed so the accounted arm stops throwing in `bench-apply.mjs`'s per-board scan loader, plus an Ashby remount leg. The fixture server **shall** gain a declared latency model (`--latency`, default ~300ms nav / 150ms XHR) reported as a **separate column never merged with loopback**, and a **parameterised employer segment** (`/boards.greenhouse.io/fixture-emp-<n>/jobs/<id>`) so a 50-app run spans ≥8 distinct origins/tenants instead of one.                                                                                                                                                                                                                                                                                                                                                                                                                                              | `qa`                        | PARTIAL     |
| **0.11** | **NEW (C12).** One `find-jobs` sweep across the current board set — **44**, all enabled, counted in `docs/job-sources.yaml` at `0b6db30`; the "46" this row used to say is unsourced and §0.2's 0.11 correction already used 44 — reporting **qualifying leads per board per sweep**, and an extrapolation stating the board count required for 999 qualifying leads/day **as a number written into §7 R-8 and §2**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `implementer`               | DONE        |
| **0.12** | **DONE 2026-08-02** (`qa`, `4650a00`) — **`docs/measurements.md` M7: 1 of 7**, and **5 of 7 permanently amber**. **This row's premise was wrong twice; both corrections are in M7.** There are no "already-stored scans of the 141 leads" — scanning is per-application and 137 leads have never been scanned, so the corpus is the 7 remembered shapes in `jobs/.field-cache.json` plus 4 workspace scans. And the predicted blocker is not the one: **not one `confirm-widget` defer is a work-auth or demographic question** — on every Greenhouse form here those render as **combos**, blocking via `CONFIRM` or not at all. Original text: Green-tier prevalence **by widget shape** — what fraction of real launch-board forms can reach green at all, given that a required work-auth or demographic question rendered as a radio group defers permanently under the settled `confirm-widget` rule. This costs no new browsing and it gates the supply math as hard as 0.11 does. | `implementer`               | DONE        |
| **0.13** | **DONE 2026-08-03** (`implementer`, `5d5da52`, M10 — recovers the embedded URLs, refutes the aggregator half). **NEW (review, F6 follow-on).** `apply_url` canonicalization at enrichment: aggregator links and embedded careers pages **shall** be resolved to the underlying ATS-hosted URL where one exists. Measured today: 47/141 leads carry `adzuna.com` and 10 carry `coinbase.com` (an embedded board whose real ATS is not the recorded registrable domain), so the trust gate as specified rejects them even when the underlying form is Greenhouse.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `implementer`               | DONE        |

**Falsifiable check.** All of the following go red on regression:
`node --test "tests/auto/**/*.test.mjs"` includes a case where `consumeSubmitToken` is called with
a `pageUrl` on a different origin from `token.apply_url` and asserts `TokenError`; a case where a
plan carrying `labelFlag` yields `submitReadiness === false`; a case asserting a defer reason built
from a hostile label contains no instruction-shaped substring. A test asserts the synced profile
directory contains no file named `Login Data` or `Web Data`.
`tests/apply/field-cache.test.mjs` asserts two different hosts with identical label sets produce
different fingerprints. **B1 exists in `docs/measurements.md` with three `file_sha1`s and a
reproducible command**, and `node scripts/dev/bench-apply.mjs --board ashby` exits 0 instead of
throwing in the scan loader. **Neither half of that sentence has been run and recorded**: B1 is
absent from `docs/measurements.md`, and the ashby arm is unblocked but unmeasured. **0.11 and 0.12 each produce a number in this document**; a phase check that
produced no number has not been done.

---
