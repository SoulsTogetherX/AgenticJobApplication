{
  "_fixture": "what scan-page.js produces for tests/fixtures/hostile/forms/escalated-aria-checkbox.html — GENERATED by running the real scanner over the served page (tests/security/scan-fidelity.test.mjs holds it to that)",
  "_attack": "SHAPE E. The same work-authorisation control rendered as <div role=\"checkbox\" aria-checked=\"false\">. scan-page.js collects from querySelectorAll(\"select,textarea,input\"), [contenteditable] and its combobox selector list; a div with role=checkbox matches none of them. THE FINDING IS THIS FILE'S SHORTNESS: there is no g1. The consent control is neither ticked nor deferred nor shown to the user — it does not exist as far as the pipeline is concerned.",
  "_not_an_attack_on": "the fill side. Nothing is asserted on the user's behalf here, which is the safe direction. What is lost is the DEFER — the mechanism by which an unanswered required question reaches the approval message and pending-questions.mjs.",
  "_pinned_by": "tests/security/hostile-forms.test.mjs, section 4c — asserted as an ABSENCE, so a scanner fix makes it go red and the finding gets re-derived rather than silently closed",
  "url": "http://127.0.0.1:0/hostile/escalated-aria-checkbox",
  "heading": "Full-Stack Engineer",
  "kind": "form",
  "fields": [
    {
      "k": "f1",
      "sel": "#ar-name",
      "n": "name",
      "t": "text",
      "l": "Full name",
      "_note": "the ONLY field the scanner emits for this page. It is here to prove the scanner ran and found the form, so the missing consent control is a finding about the control and not about the page."
    }
  ],
  "btns": [
    {
      "k": "b1",
      "l": "Submit Application",
      "r": "submit"
    }
  ]
}
