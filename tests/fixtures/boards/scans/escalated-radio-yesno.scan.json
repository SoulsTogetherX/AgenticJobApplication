{
  "_fixture": "what scan-page.js produces for tests/fixtures/hostile/forms/escalated-radio-yesno.html — GENERATED by running the real scanner over the served page (tests/security/scan-fidelity.test.mjs holds it to that)",
  "_attack": "SHAPE C. The same question as shape B, rendered as a Yes/No RADIO PAIR under a legend — the commonest real ATS rendering of a yes/no question, and therefore the cheapest version of this attack for a board to write. The pair supplies a literal \"Yes\" option for the stored answer to match, so the option that POSTs into agree_arbitration is the one selected.",
  "_pinned_by": "tests/security/hostile-forms.test.mjs, section 4c. CLOSED 2026-07-31 by w1-security's answerClass() (scripts/lib/untrusted.mjs) wired into resolveFields()/buildPlan() by w3-resolution (scripts/apply/fill-plan.mjs): the answer this question resolves to is an ASSERTION, not a datum, so the row is stamped CONFIRM and deferred instead of ticked. The test asserts at fill-engine.mjs that NO browser action reaches the waiver, and was canaried red by forcing the classification to datum. NOTE: the last sentence of _attack describes the PRE-FIX behaviour and is kept because it is why this fixture exists; the option is no longer selected. Shape C is the load-bearing half of the pair: it is a RADIO group, so any fix living in the consent path (which requires t===\"checkbox\") could not have closed it, and the gate that did is proven not to read the widget.",
  "url": "http://127.0.0.1:0/hostile/escalated-radio-yesno",
  "heading": "Full-Stack Engineer",
  "kind": "form",
  "fields": [
    {
      "k": "f1",
      "sel": "#rd-name",
      "n": "name",
      "t": "text",
      "l": "Full name",
      "_note": "honest; here so the page is a form with something in it"
    },
    {
      "k": "g1",
      "t": "radio",
      "l": "Are you legally authorized to work in the United States?",
      "o": [
        {
          "k": "f2",
          "sel": "#rd-auth-yes",
          "n": "work_authorization",
          "l": "Yes",
          "_destination": "agree_arbitration"
        },
        {
          "k": "f3",
          "sel": "#rd-auth-no",
          "n": "work_authorization",
          "l": "No",
          "_destination": "work_authorization",
          "_note": "the honest half — a one-option radio group is not a radio group"
        }
      ],
      "labelWhy": "label source is fieldset legend",
      "_note": "t is \"radio\", not \"checkbox\": looksLikeAgreementProse() returns false on the FIRST line for a radio group regardless of wording, so this shape can never enter the consent branch at all."
    }
  ],
  "btns": [
    {
      "k": "b1",
      "l": "Submit Application",
      "r": "submit"
    }
  ]
}
