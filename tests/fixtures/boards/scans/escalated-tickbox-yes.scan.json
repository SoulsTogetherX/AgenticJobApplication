{
  "_fixture": "what scan-page.js produces for tests/fixtures/hostile/forms/escalated-tickbox-yes.html — GENERATED by running the real scanner over the served page (tests/security/scan-fidelity.test.mjs holds it to that)",
  "_attack": "SHAPE B. mislabelled-escalated's work-authorisation box, re-rendered as a fieldset+legend with one tickbox whose OWN label is \"Yes\". The question text, the input name and the server destination are unchanged; only the markup differs. The stored answer for this question is the string \"Yes\", so unlike the lone-tickbox rendering there is now an option for it to match — and the box is TICKED.",
  "_pinned_by": "tests/security/hostile-forms.test.mjs, section 4c — a LANDED attack, asserted green so the security gate stays readable",
  "url": "http://127.0.0.1:0/hostile/escalated-tickbox-yes",
  "heading": "Full-Stack Engineer",
  "kind": "form",
  "fields": [
    {
      "k": "f1",
      "sel": "#tb-name",
      "n": "name",
      "t": "text",
      "l": "Full name",
      "_note": "honest; here so the page is a form with something in it"
    },
    {
      "k": "g1",
      "t": "checkbox",
      "l": "Are you legally authorized to work in the United States?",
      "o": [
        {
          "k": "f2",
          "sel": "#tb-auth-yes",
          "n": "work_authorization",
          "l": "Yes",
          "_destination": "agree_arbitration"
        }
      ],
      "labelWhy": "label source is fieldset legend",
      "_note": "no labelExact: a legend is a heading for a GROUP, so scan-page.js withholds the vouch. That is irrelevant here — the vouch gates the CONSENT branch, and this label reaches neither isConsent() nor looksLikeAgreementProse(), so it is ticked by the ordinary checkbox branch instead."
    }
  ],
  "btns": [
    {
      "k": "b1",
      "l": "Submit Application",
      "r": "submit"
    }
  ]
}
