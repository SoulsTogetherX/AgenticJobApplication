{
  "_fixture": "what scan-page.js produces for tests/fixtures/hostile/forms/mislabelled-escalated.html — GENERATED by running the real scanner over the served page (tests/security/scan-fidelity.test.mjs holds it to that)",
  "_attack": "mislabelled-inputs.scan.json with id, name and autocomplete RENAMED to agree with the lying label. Nothing in the document disagrees with anything else, so fieldIdentityMismatch() has no token left to compare and returns \"\" on all four fields. `_destination` is the column the server writes — it is NOT in the document and no scanner can recover it; the attribute exists only so a test can name the truth.",
  "_pinned_by": "tests/security/hostile-forms.test.mjs, section 4b — a LANDED attack, asserted green so the security gate stays readable",
  "url": "http://127.0.0.1:0/hostile/mislabelled-escalated",
  "heading": "Full-Stack Engineer",
  "kind": "form",
  "fields": [
    {
      "k": "f1",
      "sel": "#esc-phone",
      "n": "phone",
      "ac": "tel",
      "t": "tel",
      "l": "Phone number",
      "_destination": "ssn",
      "_note": "every token an honest phone field would carry; the value POSTs into the ssn column"
    },
    {
      "k": "f2",
      "sel": "#esc-start",
      "n": "start_date",
      "t": "text",
      "l": "Preferred start date",
      "_destination": "salary_floor"
    },
    {
      "k": "g1",
      "t": "checkbox",
      "l": "Are you legally authorized to work in the United States?",
      "o": [
        {
          "k": "f3",
          "sel": "#esc-auth",
          "n": "work_authorization",
          "l": "Are you legally authorized to work in the United States?",
          "_destination": "agree_arbitration"
        }
      ],
      "labelExact": true,
      "_note": "the only one of the four that a structural control still stops: a consent-shaped box defers on its SHAPE, which reads no token the board chose"
    },
    {
      "k": "f4",
      "sel": "#esc-contact",
      "n": "emergency_contact_phone",
      "t": "email",
      "l": "Emergency contact phone",
      "lSeen": "Email",
      "_visible_label": "Email",
      "_destination": "emergency_contact_phone",
      "_note": "the label divergence, unchanged from mislabelled-inputs — but the id no longer says \"email\", so the guard that used to catch this one no longer fires"
    }
  ],
  "btns": [
    {
      "k": "b1",
      "l": "Submit Application",
      "r": "submit"
    }
  ]
}
